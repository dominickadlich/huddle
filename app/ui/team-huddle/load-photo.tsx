// const [signedUrls, setSignedUrls] = useState<string[] | undefined>()
    // useEffect(() => {
    //     (async () => {
    //         if (huddle_id !== undefined) {
    //             const promise = await getHuddleImage({huddle_id})
    //             setSignedUrls(promise.signedUrls)
    //         }
    //     })
        // () // Call the async function immediately
    // }, [huddle_id])



    
     {/* {signedUrls && 
                        signedUrls?.map((url) => (
                            <img
                                key={url}
                                src={url}
                            />
                        ))} */}